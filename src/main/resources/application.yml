spring:
  flyway:
    clean-disabled: false
    validate-on-migrate: false
  application:
    name: marketplace-mvp
  
  datasource:
    url: jdbc:postgresql://localhost:5434/cryptodrop
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: admin
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
    mode: HTML
  
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8082
  error:
    include-message: always
    include-binding-errors: always

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.cryptodrop: DEBUG
    org.springframework.security: DEBUG

oxapay:
  enabled: ${OXAPAY_ENABLED:true}
  sandbox-enabled: ${OXAPAY_SANDBOX_ENABLED:true}
  sandbox-merchant: ${OXAPAY_SANDBOX_MERCHANT:sandbox}
  base-url: ${OXAPAY_BASE_URL:https://api.oxapay.com}
  merchant-api-key: ${OXAPAY_MERCHANT_API_KEY:NDVCVU-EVE3JI-I0GHHV-OLC9X7}
  payout-api-key: ${OXAPAY_PAYOUT_API_KEY:}
  callback-url: ${OXAPAY_CALLBACK_URL:http://localhost:8082/api/checkout/oxapay/callback}
  return-url: ${OXAPAY_RETURN_URL:http://localhost:8082/orders}
  invoice-lifetime-minutes: ${OXAPAY_INVOICE_LIFETIME_MINUTES:30}
  currency: ${OXAPAY_CURRENCY:USD}
