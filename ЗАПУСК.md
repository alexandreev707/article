# 🚀 Инструкция по запуску CryptoDrop MVP

## Быстрый старт (3 команды)

### 1. Запустить инфраструктуру (PostgreSQL + Kafka)
```bash
docker-compose up -d
```

### 2. Запустить Backend (в отдельном терминале)
**Windows:**
```bash
gradlew.bat bootRun
```

**Linux/Mac:**
```bash
./gradlew bootRun
```

### 3. Запустить Frontend (в отдельном терминале)
```bash
cd frontend
npm install  # только первый раз
npm run dev
```

## ✅ Проверка работы

1. **Backend:** Откройте http://localhost:8080/api/actuator/health
   - Должен вернуть: `{"status":"UP"}`

2. **Frontend:** Откройте http://localhost:3000
   - Должна загрузиться главная страница

3. **Подключите кошелек:**
   - Нажмите "Подключить кошелек"
   - Выберите Phantom или Solflare
   - Подтвердите подключение

## 📝 Тестовый сценарий

### Как продавец:
1. Перейдите в "Продавцу"
2. Нажмите "+ Добавить товар"
3. Заполните форму и создайте товар

### Как покупатель:
1. Вернитесь на главную страницу
2. Найдите товар и нажмите на него
3. Нажмите "Купить за USDC"
4. Подтвердите транзакцию в кошельке

### Проверка заказов:
1. Перейдите в "Мои заказы"
2. Должен отображаться созданный заказ

## 🐛 Решение проблем

### PostgreSQL не запускается
```bash
docker-compose logs postgres
docker-compose restart postgres
```

### Порт 8080 занят
Измените порт в `src/main/resources/application.yml`:
```yaml
server:
  port: 8081
```

### Frontend не подключается к API
1. Убедитесь что Backend запущен
2. Проверьте консоль браузера (F12) на ошибки

### Кошелек не подключается
1. Установите расширение Phantom или Solflare
2. Убедитесь что кошелек разблокирован

## 📚 Дополнительная информация

- Полная документация: `README.md`
- Детальное руководство: `QUICKSTART.md`
- Тестирование: `TESTING.md`

## 🔧 Структура проекта

```
.
├── src/main/kotlin/     # Backend (Spring Boot + Kotlin)
├── frontend/            # Frontend (React + TypeScript)
├── docker-compose.yml   # Инфраструктура (PostgreSQL, Kafka)
└── build.gradle.kts     # Конфигурация Gradle
```

## ⚙️ Переменные окружения (опционально)

Создайте `.env` файл или установите переменные:

```env
SOLANA_ESCROW_PROGRAM_ID=your_program_id
SOLANA_PLATFORM_WALLET=your_wallet_address
DHL_API_KEY=your_dhl_key
```

Или используйте значения по умолчанию из `application.yml`.

## 🎯 Что дальше?

После успешного запуска:
1. Изучите API endpoints в `README.md`
2. Протестируйте функциональность через браузер
3. Проверьте логи в консоли Backend
4. Изучите структуру БД через `docker exec -it cryptodrop-postgres psql -U postgres -d cryptodrop`

---

**Готово!** 🎉 Проект должен работать. Если возникли проблемы - проверьте логи или обратитесь к документации.
