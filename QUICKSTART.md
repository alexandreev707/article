# 🚀 Быстрый старт CryptoDrop MVP

## Шаг 1: Подготовка окружения

### Установите необходимые инструменты:

1. **Java 17+**
   ```bash
   java -version
   ```

2. **Node.js 18+**
   ```bash
   node -v
   npm -v
   ```

3. **Docker Desktop**
   - Скачайте с https://www.docker.com/products/docker-desktop
   - Убедитесь что Docker запущен

## Шаг 2: Клонирование и настройка проекта

```bash
# Если проект еще не склонирован
git clone <repository-url>
cd article

# Убедитесь что вы в корне проекта
ls -la
# Должны увидеть: build.gradle.kts, docker-compose.yml, frontend/
```

## Шаг 3: Запуск инфраструктуры (PostgreSQL + Kafka)

### Windows:
```bash
start.bat
```

### Linux/Mac:
```bash
chmod +x start.sh
./start.sh
```

### Или вручную:
```bash
docker-compose up -d
```

**Проверка:**
```bash
docker-compose ps
# Должны быть запущены: cryptodrop-postgres, cryptodrop-kafka, cryptodrop-zookeeper
```

## Шаг 4: Запуск Backend

### Терминал 1 (Backend):

**Windows:**
```bash
gradlew.bat bootRun
```

**Linux/Mac:**
```bash
./gradlew bootRun
```

**Ожидаемый вывод:**
```
Started CryptoDropApplication in X.XXX seconds
```

**Проверка:**
```bash
# В другом терминале или браузере
curl http://localhost:8080/api/actuator/health
# Должен вернуть: {"status":"UP"}
```

## Шаг 5: Запуск Frontend

### Терминал 2 (Frontend):

```bash
cd frontend
npm install  # Только первый раз
npm run dev
```

**Ожидаемый вывод:**
```
  VITE v5.x.x  ready in XXX ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

## Шаг 6: Тестирование

### 1. Откройте браузер
```
http://localhost:3000
```

### 2. Подключите кошелек
- Нажмите "Подключить кошелек"
- Выберите Phantom или Solflare
- Подтвердите подключение

### 3. Создайте тестовый товар (как продавец)
- Перейдите в "Продавцу"
- Нажмите "+ Добавить товар"
- Заполните форму:
  - Название: "Test Product"
  - Описание: "Test Description"
  - Цена: 99.99
  - Изображения: https://via.placeholder.com/300
- Нажмите "Создать товар"

### 4. Купите товар (как покупатель)
- Вернитесь на главную страницу
- Найдите созданный товар
- Нажмите на товар
- Нажмите "Купить за USDC"
- Подтвердите транзакцию в кошельке

### 5. Проверьте заказы
- Перейдите в "Мои заказы"
- Должен отображаться созданный заказ

## 🐛 Решение проблем

### Проблема: PostgreSQL не запускается

```bash
# Проверить логи
docker-compose logs postgres

# Пересоздать контейнер
docker-compose down -v
docker-compose up -d postgres
```

### Проблема: Порт 8080 занят

Измените порт в `src/main/resources/application.yml`:
```yaml
server:
  port: 8081
```

И обновите `frontend/vite.config.ts`:
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8081',
  }
}
```

### Проблема: Frontend не подключается к API

1. Убедитесь что Backend запущен на порту 8080
2. Проверьте `frontend/vite.config.ts` - proxy должен указывать на правильный порт
3. Проверьте консоль браузера (F12) на ошибки CORS

### Проблема: Кошелек не подключается

1. Убедитесь что установлен Phantom или Solflare расширение
2. Проверьте что кошелек разблокирован
3. Для разработки можно использовать Solana Devnet (измените в `WalletContext.tsx`)

### Проблема: npm install не работает

```bash
# Очистить кэш
npm cache clean --force

# Удалить node_modules и package-lock.json
rm -rf node_modules package-lock.json  # Linux/Mac
rmdir /s node_modules package-lock.json  # Windows

# Переустановить
npm install
```

## ✅ Чеклист готовности

- [ ] Docker запущен и работает
- [ ] PostgreSQL контейнер запущен (`docker-compose ps`)
- [ ] Kafka контейнер запущен
- [ ] Backend запущен на порту 8080
- [ ] Health endpoint отвечает (`/api/actuator/health`)
- [ ] Frontend запущен на порту 3000
- [ ] Браузер открывает `http://localhost:3000`
- [ ] Кошелек подключается
- [ ] Можно создать товар
- [ ] Можно создать заказ

## 📚 Дополнительная информация

- Полная документация: `README.md`
- Руководство по тестированию: `TESTING.md`
- API endpoints: см. раздел "API Endpoints" в README.md
